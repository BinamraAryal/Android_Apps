<!-- rounded_edittext_bg.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#FFFFFF" /> <!-- Background color -->
    <corners android:radius="20dp" /> <!-- Radius for round edges -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp" /> <!-- Optional padding -->
    <stroke
        android:width="1dp"
        android:color="#BDBDBD" /> <!-- Optional border -->
</shape>
